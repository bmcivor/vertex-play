# I-01 — App repo baseline

- **Remote `origin`:** `git@github.com:bmcivor/vertex-play.git`
- **This checkout:** branch `initial-implementation` (local merge target: `origin/master`)
- **GitHub default branch:** confirm in repo settings if CI or docs must name it
- **`applicationId` / namespace:** TBD until `app/` + Gradle exist
